import java.util.Random();

public static void Loot() {

  public void run() {
    Random loot = new Random();
    
    public int getLoot(Player id);
    int gold = id.getGold;
    gold += loot.nextint(10);
    Player.setGold(gold);
  }
}
